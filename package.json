{
  "name": "lfgm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "express": "^4.17.0",
    "mysql2": "^1.6.5",
    "sequelize": "^5.8.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.0.6",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.19.0"
  },
  "scripts": {
    "start": "nodemon --watch src src/index.js --exec babel-node --presets babel-preset-env",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist --presets babel-preset-env",
    "serve": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC"
}
